pipeline {
    agent {
        kubernetes {
            defaultContainer 'default'
        }
    }
    stages {
        stage('Docker Test') {
            steps {
                sh 'docker --version'
                sh 'docker ps'
            }
        }
    }
}